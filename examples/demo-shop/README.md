# demo-shop

A minimal fake checkout app used to record the canonical workflow:
`Login -> Search -> Add to cart -> Checkout -> Success`. Includes a lookalike
"ad" link so a failing agent has somewhere to diverge.

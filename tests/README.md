# Tests

- `e2e/` — Playwright tests that drive AgentReplay's own dashboard (record -> compile -> run -> diverge -> summarize).
- `integration/` — backend pipeline tests (events -> graph -> run -> metrics).

Backend unit tests live next to the code in `apps/api/tests`.
